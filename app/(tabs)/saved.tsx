import { View, Text } from 'react-native';

export default function SavedScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#FEFADF', justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 18 }}>Saved Posts</Text>
    </View>
  );
}
